# 屏柜模块（前端）

只读列举屏柜系统（ct-screen）数据，路由 `/admin/screen/cabinets/*`。

认知内容在 `../business/display/` 维护（业务库 ct-screen-monitor，菜单名「屏柜学习」）。

对应后端：
- 只读：`com.zeta.screen` + `/api/knowledge/*`
- 展示补充：`com.zeta.business` + `/api/cabinets/{id}/display-items`
