/**
 * 屏柜系统只读接入（ct-screen）。
 *
 * <p>按实体分包：
 * <ul>
 *   <li>{@code cabinet} — 屏柜</li>
 *   <li>{@code ieddevice} — IED 保护装置</li>
 *   <li>{@code logicdiagram} — 保护逻辑框图</li>
 *   <li>{@code knowledge} — 知识结构聚合查询 API</li>
 * </ul>
 */
package com.zeta.screen;
